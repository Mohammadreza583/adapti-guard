# Frozen Evaluation Datasets

**Status:** `FREEZE` for `eval_v1`; `layer_a_v2` is historical; `layer_a_v3` is the current Layer A mixed PI pack.

- `eval_v1/` — original 770-row attack-only set (unchanged)
- `layer_a_v2/` — historical 40 PI + 40 benign Layer A pack (do not modify)
- `layer_a_v3/` — 80 PI + 80 benign with train/dev/test (`DATASET_CARD.md`)
