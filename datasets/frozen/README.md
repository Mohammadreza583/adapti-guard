# Frozen Evaluation Datasets

**Status:** `FREEZE` for `eval_v1`; `layer_a_v2` is an additional frozen mixed PI pack.

- `eval_v1/` — original 770-row attack-only set (unchanged)
- `layer_a_v2/` — 40 PI + 40 benign for Layer A hardening (`DATASET_CARD.md`)
