# Overlap Matrix

Generated: 2026-08-29T15:36:31.416191+00:00

- Papers profiled: **55/55** (Phase11 proxy; local TOTAL-- TXT absent)
- Direct competitors: **0**
- Partial overlap: **13**
- High-overlap within partial: **SafeHarness (P46)**

## Named competitors (short)
| Paper | Overlap | Shared | Missing vs AG |
| ----- | ------- | ------ | ------------- |
| SafeHarness | PARTIAL/HIGH — discrete escalate/de-escalate privilege 0–4 + safe-window recovery | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| AgentAntibody | PARTIAL — runtime immune memory responses | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| SCOUT | PARTIAL — detector allocation safety–utility–latency | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| COPA | PARTIAL — continual training-time adaptation | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| HARD | PARTIAL — self-evolving harness from failures | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| Task Shield | PARTIAL — test-time task alignment | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| PIGuard | PARTIAL — over-defense-aware detector | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| VIGIL | PARTIAL — verify-before-commit tool-stream defense | runtime adapt / SUC axis / agent PI | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| AutoDojo | COMPLEMENTARY — adaptive attack optimizer (not defense) | eval/threat adjacency | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| Adaptive Adversaries | COMPLEMENTARY — adaptive attacker bench | eval/threat adjacency | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| SecOPD | ADJACENT — training-time token distillation | eval/threat adjacency | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| MELON | ADJACENT — masked re-execution detection | eval/threat adjacency | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |
| Spotlighting | BACKGROUND — fixed prompting transform | eval/threat adjacency | AG L0–L3 sanitize/restrict/block + legitimate-cost de-esc + 75/25 fixed-vs-adaptive SUC |

## Strict DIRECT test
DIRECT requires: discrete runtime intervention levels + attack escalation + legitimate-cost de-escalation + mixed fixed-vs-adaptive SUC protocol.
**No paper meets all.** SafeHarness is closest on discrete escalate/de-escalate only.

## Corpus caveat
`TOTAL--` TXT folders not present in VM; matrix inherits Phase11 verified public extracts. Re-run Phase2 when TXTs are mounted.
