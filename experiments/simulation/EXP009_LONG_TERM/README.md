# EXP009_LONG_TERM

Experiment artifact directory (packaged 2026-09-04).

Primary files present at packaging time:

- `config.json`
- `metrics.json`
- `run_metadata.json`

Scientific metrics are only those recorded in `metrics.json` / nested result files — never inferred.
