# گام بعدی برای متین (دو مسیر؛ فقط انسان)

این یادداشت نتیجهٔ علمی را عوض نمی‌کند. مجوز مرج، سابمیت، یا ارزیابی زنده نیست.

عامل هوش مصنوعی مرج نمی‌کند، ونیو انتخاب نمی‌کند، مقاله را به ورک‌شاپ یا arXiv نمی‌فرستد، ارزیابی زندهٔ جدید اجرا نمی‌کند، آشکارساز را دوباره تنظیم نمی‌کند، و بسته‌های منجمد یا اعداد FAIL مسیر A را ویرایش نمی‌کند.

دو مسیر جدا هستند. قاطی کردن آن‌ها در یک جملهٔ «آداپتی‌گارد کار می‌کند / شکست خورد» خطای ادعا است.

## مسیر A — تأیید VNEXT (FAIL؛ قفل)

تأیید VNEXT برابر **FAIL** است. بردِ واجدشرایط (H1) برابر **خیر** است. مداخلهٔ تطبیقیِ هزینه‌آگاه در زمان اجرا **تأیید نشده** است.

- بسته: `vnext_confirm_v1.0` با SHA-256 برابر `523c881820710783b5290c76ea5fe5fc01a6341fb427defcba1119fc3e721518`
- ASR پایه (B0): 0.9508 ؛ درمان VNEXT-ADAPT: 0.8689
- مک‌نمار: b10 = 5 ، b01 = 0 ، p = 0.0625 (معنادار نیست)
- اثر در برابر MSID: δ̂ = 0.0820 که از 0.20 کمتر است
- سودمندی: U = 0.9344 که از 0.95 کمتر است
- پرونده: `experiments/real_llm_eval/VNEXT_CONFIRM/20260914-133147/AUDIT.md`

عدد p = 0.0625 را تأیید نخوانید. این FAIL را PARTIAL یا روندِ پیروزی ننامید. مسیر B این FAIL را برعکس نمی‌کند.

## مسیر B — تأیید زندهٔ فاز۱ (ادعای محدود)

طبقه‌بندی **SUPPORTED_IMPROVEMENT** است؛ فقط روی بستهٔ `phase1_confirm_v1` و درمان PHASE1-CORE در برابر B0. این نتیجهٔ VNEXT نیست.

- اجرا: `phase1_confirm_20260914T213022Z_a2681e92` (پی‌آر #39 پیش‌نویس)
- SHA-256 بسته: `c789811a07d3ed06e1c77d8a45eda6172f480226e006d84fa28386a982536d01`
- B0 = 1.0000 ؛ CORE = 0.5574 ؛ δ̂ = 0.4426 (حدود 0.443) ؛ p حدود 1.49e-8 ؛ b10/b01 = 27/0 ؛ U حدود 0.9672
- پرونده: `experiments/real_llm_eval/PHASE1_CONFIRM/phase1_confirm_20260914T213022Z_a2681e92/AUDIT.md`

نباید گفت SOTA، آمادهٔ تولید، یا «تزریق پرامپت حل شد». نباید گفت VNEXT دیگر PASS است.

جزئیات ادعا: `docs/paper/CLAIMS_DUAL_TRACK.md`. وضعیت کوتاه انگلیسی: `docs/paper/DUAL_TRACK_STATUS.md`.

## ترتیب پیشنهادی مرج (فقط شما اجرا می‌کنید)

فهرست کامل در `docs/paper/workshop_vnext_fail/PR_STACK.md` است. عامل‌ها از روی فایل مرج نمی‌کنند.

1. در صورت نیاز #22 سپس #23 تا #28 (مستندات، harness، بستهٔ قفل‌شده؛ JSONL را بازنویسی نکنید)
2. #30 پیش‌زنده؛ **#29 را مرج نکنید** (رانر استفاده‌نشده؛ FAIL رسمی نیست)
3. #31 ارزیابی زندهٔ رسمی FAIL مسیر A (اعداد الزام‌آور همین‌جاست)
4. #32 متن ورک‌شاپ ؛ #33 بستن ؛ #34 بستهٔ سابمیت مسیر A (سابمیت خودکار نیست)
5. #35 هستهٔ فاز۱ آفلاین ؛ #37 قفل holdout/آشکارساز ؛ #38 سخت‌سازی علمی
6. **#36 را در مسیر فاز۱ مرج نکنید** مگر پروتکل فاز۲ را جدا بخواهید (پیاده‌سازی فاز۲ نیست)
7. #39 پیش‌نویس نتایج زندهٔ مسیر B
8. #40 بهداشت دو-مسیره (مستندات ادعا؛ روی نوک #39)
9. #41 بهداشت مخزن (نقشهٔ خواندن + CLOSE / SKIP + حذف `.venv` از ایندکس؛ روی نوک #40)

## CLOSE / SKIP (فقط توصیه؛ با API اجرا نکنید)

عامل‌ها پی‌آر را با GitHub API نمی‌بندند و مرج نمی‌کنند. فقط شما.

- **CLOSE یا SKIP #29:** رانر موازی و استفاده‌نشدهٔ VNEXT؛ FAIL رسمی نیست.
- **SKIP مرج #36** در مسیر Track B مگر پروتکل فاز۲ را جدا بخواهید (هم‌نیا با #37). پیاده‌سازی فاز۲ نیست.
- **KEEP:** … → #35 → #37 → #38 → #39 → #40 → #41.
- مرج ساده پس از بازبینی: نوک پشته را یک‌بار بنشانید، یا squash-merge به `main`. عامل‌ها هرگز مرج نمی‌کنند.
- بازنویسی تاریخ برای پاک کردن blobهای قدیمی `.venv` خارج از این کار است.

#29 و #30 هر دو روی #28 سوارند. امتیازدهی رسمی VNEXT مسیر #30 سپس #31 بوده است.

## کارهایی که نباید بکنید

- مرج از طرف عامل یا از روی این فایل
- سابمیت به ورک‌شاپ یا arXiv بدون تصویب صریح شما
- ارزیابی زندهٔ جدید یا فراخوان OpenRouter
- تنظیم دوبارهٔ آشکارساز؛ افزایش N؛ ویرایش JSONL قفل‌شده
- تغییر اعداد FAIL مسیر A یا برعکس‌خوانی VNEXT
- احیای ادعاهای قدیمی Q1 به‌عنوان نتیجهٔ تأییدی فعلی
- ادعای SOTA، آمادهٔ تولید، یا حل تزریق پرامپت

اسناد Q1 تاریخی‌اند؛ ادعای جاری نیستند.
