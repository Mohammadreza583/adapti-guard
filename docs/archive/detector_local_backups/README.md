# Detector local backups

Historical `prompt_injection_detector.py.*` copies that were tracked beside the live detector. They are **not** imported. Live code: `src/adapti_guard/detector/prompt_injection_detector.py` plus `_v4` / `_phase1`. Do not retune from these files.
