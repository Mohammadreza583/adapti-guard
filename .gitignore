# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

# Virtual environment
.venv/

# Backup files
*.bak
*.backup
*.cp*_backup*
*.cp*_robust_backup_*

# Repair backups
.repair_backups/

# Logs
logs/
*.log

# Experiment outputs
# Default: ignore all results/ (regenerable or stored locally).
results/
!results/summaries/
!results/summaries/**
!results/manifests/
!results/manifests/**
# Raw per-run artifacts (responses, logs) remain gitignored under experiment_runs/
# Commit only aggregate metrics via results/summaries/ and manifests/

# LLM request cache (regenerable; may contain model outputs)
.llm_cache/

# Environment
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Local datasets and embedded repositories
BIPIA/
dataset/
datasets/
!datasets/benchmark_v2/
!datasets/benchmark_v2/schema.json
!datasets/benchmark_v2/manifest.json
!datasets/benchmark_v2/README.md
!datasets/benchmark_q1/
!datasets/benchmark_q1/dataset_card.md
!datasets/benchmark_q1/statistics.json
!datasets/benchmark_q1/hashes.json
!datasets/benchmark_q1/distribution_report.md
!datasets/benchmark_q1/schema.json
backups/

# Generated project reports
full_project_report.txt
project_report.txt

# All backup variants
*.bak*
*.backup*
*.cp*_backup*
*.cp*_robust_backup_*

# Temporary accidentally generated files
t -q -p no:langsmith

# Additional backup naming patterns
*_backup
*_backup.*

# Additional backup naming patterns
*_backup
*_backup.*

# Local detector backups / historical versions
src/adapti_guard/detector/*.bak
src/adapti_guard/detector/*.v*
src/adapti_guard/detector/*_backup*
